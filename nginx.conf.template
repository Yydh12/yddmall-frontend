server {
  listen ${PORT};
  server_name _;
  root /usr/share/nginx/html;

  # 静态资源缓存
  location ~* \.(?:js|css|png|jpg|jpeg|gif|svg|ico|woff2?)$ {
    try_files $uri =404;
    expires 7d;
    access_log off;
  }

  # SPA 路由回退
  location / {
    try_files $uri $uri/ /index.html;
  }
}

